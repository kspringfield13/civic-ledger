Alembic migrations land in Phase 1 (`alembic init` + baseline autogenerate
against the models in `app/models`). Kept empty in the scaffold so the
baseline reflects the first reviewed schema, not placeholder churn.
